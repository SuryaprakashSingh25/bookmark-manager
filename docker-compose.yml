version: "3.9"

services:
  postgres:
    image: postgres:17
    container_name: bookmark-postgres

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: bookmarkdb

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

  preview-service:
    build: ./preview-service

    container_name: preview-service

    ports:
      - "50051:50051"

  api:
    build: ./api

    container_name: bookmark-api

    ports:
      - "8080:8080"

    environment:
      - GRPC_PREVIEW_ADDR=preview-service:50051

    depends_on:
      - postgres
      - preview-service

  frontend:
    build: ./frontend
    
    container_name: bookmark-frontend

    ports:
      - "4200:80"

    depends_on:
      - api

volumes:
  postgres_data: